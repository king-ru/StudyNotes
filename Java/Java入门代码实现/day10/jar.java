/*
Jar包（register注册）
Java的压缩包
1.方便项目的携带。
2.方便于使用，只要在classpath设置jar路径即可。
3.数据库驱动，SSH框架等都是以jar包体现的。

jre：Java运行环境。
jdk为什么含有虚拟机？
jdk里面都是编译工具，编译工具都是由类写的，类需要虚拟机来运行。
所以jdk里面必须内置一个jre。

将jar包放在classpath下，jar里面的程序都可以运行。
*/
class  
{
	public static void main(String[] args) 
	{
		System.out.println("Hello World!");
	}
}
